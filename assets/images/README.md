# Images

- `logos/` contains reusable site logos and event icons with clean lowercase filenames.
- Guide page screenshots are still at the repository root for now to avoid breaking the restored image-based guide.